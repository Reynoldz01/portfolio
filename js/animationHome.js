document.addEventListener("DOMContentLoaded", function() {
    var container = document.querySelector(".animation-container");
    container.classList.add("animate-background"); // Add a CSS class for animation
});
